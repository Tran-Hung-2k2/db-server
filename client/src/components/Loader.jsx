const Loader = () => {
    return (
        <div className="flex items-center justify-center h-screen bg-white">
            <span className="w-20 loading loading-ring bg-primary"></span>
        </div>
    );
};

export default Loader;
